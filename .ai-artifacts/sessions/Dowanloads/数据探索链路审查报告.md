# explorer-sql-audit审查SQL模板与探索接口

你是量化平台的代码审查员。项目位于 /Users/victor/Desktop/coding/localquant（Python 3.12 + uv，FastAPI 后端在 backend/）。这是一个只读审查任务：**绝对不要修改项目内任何文件**，临时脚本只能写到 /tmp。不要调用任何付费 AI 接口。不要启动占用 8000 端口的服务（另一个后端实例可能已在运行；优先直接 import 服务层函数测试，若必须起服务请用 8001 端口）。 背景：数据探索模块（前端「数据探索」页）提供 SQL 查询、字段字典、查询模板。2026-08 曾有审查发现「explorer.py 内置 SQL 模板 ORDER BY index 全挂」（已知问题，后在 commit abb2d8f 声称修复：统一行情视图）。本地行情缓存在 data/cache/1d/*.parquet（125 只），参考快照在 data/cache/reference/。 你的任务：实际执行验证数据探索链路，给出证据清单。 具体步骤建议： 1. 读 backend/routes/explorer.py 与 backend/services/duckdb_service.py（或类似文件），找到：a) 内置查询模板清单（名称+SQL）；b) 字段字典定义；c) SQL 执行入口（写关键字拒绝、2000 行限制的逻辑）。 2. 写 /tmp 脚本：用生产同一入口逐条执行全部内置查询模板（不走 AI），记录每条成功/失败与错误原文；特别验证 ORDER BY、日期区间、多表视图类模板。 3. 验证字段字典与实际视图表结构是否一致：字典里宣称存在的字段在 DuckDB 视图里是否真的存在（抽 10 个以上字段核对，用 PRAGMA/DESCRIBE 或 information_schema）。 4. 验证 SQL 安全边界行为：a) 尝试一条写语句（如 CREATE TABLE t(x int)）确认被拒绝；b) 大结果集是否被限制 2000 行；c) 错误时是否返回结构化 code（invalid_query 等）。 5. 若存在「异常检测」「横截面分析」「全市场扫描」接口（同文件或相邻路由），直接调用服务层函数跑一次小样本，观察是否报错或输出明显异常（如全空、NaN 满屏）。 6. 检查 /api/data/freshness 相关的交易日历逻辑（可能在 backend/services/market_data.py 或 data 路由）：在缓存内数据上执行，观察是否正确报告最新交易日；对照缓存里真实的最新日期（用 pandas 读一两个 parquet 确认末日）。 报告格式（600 字以内结论 + 明细）： - 模板执行结果表：模板名 | 成功/失败 | 错误摘要 - 字段字典与真实 schema 不一致清单 - 安全边界验证结果 - 探索类接口的实际输出问题（附文件:行号） - 交易日历/时效检查结果

2026-08-30 23:12

I'll start by exploring the project structure and reading the key files mentioned in the task.

5执行工具 5 次

Now let me read the DuckDB service and market_data service:

2执行工具 2 次

Now let me check the market_data service for freshness/calendar logic, and also check what parquet files exist:

3执行工具 3 次

Now let me write and run the comprehensive verification script:

3执行工具 3 次，其中 1 次失败

Good. Now let me write the comprehensive verification script:

2执行工具 2 次

Now let me verify a few additional edge cases — the `code` field in explorer route's `_SQL_FORBIDDEN` regex vs duckdb_service's duplicate, and check whether the `truncated` flag is missing when not truncated:

1执行工具 1 次

Now let me check the `turnover` field discrepancy more carefully and verify whether any parquet actually has it:

1执行工具 1 次

Let me also verify the SQL template dates match actual data, and check whether `000300.SH` (index) exists in cache:

1执行工具 1 次

Now let me check the `000025.SZ` outlier (latest date 2025-09-12) and verify whether the explorer's `/api/data/freshness` route exists:

2执行工具 2 次

Now let me verify one more thing — the `_SQL_FORBIDDEN` regex is duplicated in both `explorer.py` and `duckdb_service.py`. Let me also check if there's a bypass via mixed case or comments:

1执行工具 1 次

Good, the forbidden keyword regex catches these. Let me also verify one edge case about the `duckdb_service.query_local` not returning a `code` field in error responses (explorer route adds it at line 143):

1执行工具 1 次

All data collected. Here is the complete audit report:

------

## 数据探索链路审查报告

### 1. 模板执行结果表

| 模板 ID         | 名称                       | 结果 | 行数 | 错误摘要 |
| :-------------- | :------------------------- | :--- | :--- | :------- |
| recent_daily    | 单只股票最近30个交易日日线 | OK   | 30   | -        |
| limit_move_scan | 某日全市场收盘价榜         | OK   | 50   | -        |
| volume_spike    | 某日成交量前50             | OK   | 50   | -        |
| index_ohlc      | 指数最近60日               | OK   | 60   | -        |

**结论**: 4 条内置 SQL 模板全部成功执行。ORDER BY 正常（commit abb2d8f 修复已生效），日期 `2026-08-07` 与缓存最新日期一致，`quotes_1d` 视图正确映射了 trade_date/code 列。

### 2. 字段字典与真实 schema 不一致清单

| 问题                            | 详情                                                         |
| :------------------------------ | :----------------------------------------------------------- |
| `turnover` 在字典中但不在视图中 | `FIELD_DICTIONARY` (explorer.py:226) 声明了 `turnover`(换手率)，但所有 parquet 文件的列为 `[open, high, low, close, volume, amount, adjust_factor]`，无 turnover 列。DuckDB 视图 `quotes_1d` 也不含此列。**用户按字典写 `SELECT turnover FROM quotes_1d` 会报错**。 |

其余 9 个字典字段 (trade_date, code, open, high, low, close, volume, amount, adjust_factor) 均与 DuckDB 视图完全匹配。

### 3. 安全边界验证结果

| 测试项                                       | 结果         | 说明                                                         |
| :------------------------------------------- | :----------- | :----------------------------------------------------------- |
| CREATE/DROP/INSERT/DELETE/ATTACH/COPY/PRAGMA | 全部 BLOCKED | 双重检查：先验白名单(startswith select/with/describe/show)，再验黑名单正则 |
| SQL 注释注入 (`/* DROP */`)                  | BLOCKED      | 正则匹配到关键字                                             |
| 分号链接 (`;DROP TABLE`)                     | BLOCKED      | 正则匹配到 drop                                              |
| 2000 行限制                                  | 正确执行     | 请求 5000 行返回 2000，truncated=True                        |
| 错误结构化 code                              | 部分缺失     | `duckdb_service.query_local()` 返回 error 时不含 `code` 字段；`explorer.py:143` 补了 `code=query_error`。但若其他路由直接调用 `query_local` 则缺少 code。**低风险，当前唯一调用方 explorer 已补齐**。 |

### 4. 探索类接口实际输出

| 接口                               | 结果          | 备注                                                      |
| :--------------------------------- | :------------ | :-------------------------------------------------------- |
| market_scan (close>10, 2026-08-07) | OK, 108 行    | 正常过滤，数据非空                                        |
| cross_section (close, 2026-08-07)  | OK, 119 只    | count=119 < 125(总缓存)，6只缺该日数据(停牌/未上市)属正常 |
| anomaly_detection (000001.SZ, z>2) | OK, 91 条异常 | 输出合理，z-score 值域正常                                |

未发现全空、NaN 满屏等异常输出。

### 5. 交易日历/时效检查

| 项目             | 值              | 评估                                                         |
| :--------------- | :-------------- | :----------------------------------------------------------- |
| freshness.status | stale           | 符合预期 — 缓存止于 2026-08-07，距今 23 天                   |
| latest_date      | 2026-08-07      | 与 parquet 实测一致(10 只抽检全部吻合)                       |
| calendar_source  | weekday_approx  | QMT 不可用(macOS)，无持久化日历，退化为工作日近似            |
| stale_count      | 125/125         | 全部标记滞后(stale_trade_days=15)，因工作日近似高估了滞后天数 |
| 000025.SZ 异常   | 末日 2025-09-12 | 比主流数据早近一年，可能退市/停牌，freshness 未单独区分此类情况 |

**关键发现**: 交易日历退化到工作日近似后，`stale_trade_days=15` 可能高估（实际 A 股 8/7 至 8/30 约 16 个交易日，含周末则自然日 23 天）。逻辑本身无误，但 `calendar_unverified=True` 提示了可信度降级。

### 总结

- **模板 ORDER BY 修复已验证生效**，4 条模板全部可执行
- **唯一 schema 不一致**: `turnover` 字段在字典中声明但实际不存在于任何 parquet/视图（explorer.py:226）
- **SQL 安全**: 写操作阻断完备，行数限制正常
- **探索接口**: scan/cross-section/anomaly 均正常工作，无数据质量问题
- **时效检查**: 逻辑正确，但无交易日历时 stale 判定偏保守；000025.SZ 数据断档未被单独标注